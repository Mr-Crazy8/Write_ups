# Stealth Scan Configuration
# OSINT-safe reconnaissance with minimal footprint

TARGET=""
OUTPUT_DIR="./stealth_results"
THREADS=3
TIMEOUT=45
VERBOSE=false
DEBUG=false

# Conservative module selection
MODULES_ENABLED="subdomain,tech_detection"

# Stealth settings
SUBDOMAIN_PASSIVE_ONLY=true
PORT_SCAN_AGGRESSIVE=false
OSINT_SAFE_MODE=true
RESPECT_ROBOTS_TXT=true

# Rate limiting
RATE_LIMIT=2
MAX_REQUESTS_PER_HOST=10

# Minimal reporting
REPORT_FORMAT="txt"
EXECUTIVE_SUMMARY=true
ANONYMIZE_DATA=true

# Conservative resource usage
MAX_CPU_USAGE=30
MAX_MEMORY_USAGE=30
RESOURCE_MONITORING=true

# Security-focused
SECRET_DETECTION=true
VULNERABILITY_SCAN_ENABLED=false

# No notifications to avoid detection
NOTIFICATION_ENABLED=false

# Stealth user agent
USER_AGENT="Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36"

# Conservative timeouts
CONNECTION_TIMEOUT=30
MAX_RETRIES=1
RETRY_DELAY=10